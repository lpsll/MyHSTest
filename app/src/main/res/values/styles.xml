<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 带有遮罩层的Dialog Loading效果 -->
    <style name="CommonLoadingShadeDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- button send style -->
    <style name="UserBtSendStyle">
        <item name="android:layout_width">@dimen/ds_240</item>
        <item name="android:layout_height">@dimen/ds_90</item>
        <item name="android:layout_marginLeft">@dimen/ds_30</item>
        <item name="android:textColor">@color/color_00</item>
        <item name="android:background">@drawable/getcode</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLines">2</item>
        <item name="android:textSize">@dimen/fs_42</item>
    </style>

</resources>
